/*
import { moduleForComponent, test } from 'ember-qunit';
import hbs from 'htmlbars-inline-precompile';

moduleForComponent('kwoter-error', 'Integration | Component | kwoter error', {
  integration: true
});

Commenting out for now.  Button is disabled so error messages wont be rendered.
Though ideally the UI should give clues as to why the button is disabled.

test('it renders the errors attribute and message', function(assert) {
  assert.expect(1);

  let error = {
    attribute: 'nastyAttribute',
    message: 'you forgot to add text you dummy'
  };

  this.set('error',error);

  this.render(hbs`{{kwoter-error error=error}}`);

  var foundError = $('.kwoter-error');
  assert.equal(foundError.text().trim(), `${error.attribute}: ${error.message}`);
});
*/
